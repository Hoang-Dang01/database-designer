USE MarsColony;

-- 1. Xem dữ liệu bảng Tàu vũ trụ (E_JET)
SELECT * FROM E_JET;

-- 2. Xem dữ liệu bảng Nơi ở (HOUSE)
SELECT * FROM HOUSE;

-- 3. Xem dữ liệu bảng Công việc (JOB)
SELECT * FROM JOB;

-- 4. Xem dữ liệu bảng Phi công (PILOT)
SELECT * FROM PILOT;

-- 5. Xem dữ liệu bảng Chuyến bay (TRIP)
SELECT * FROM TRIP;

-- 6. Xem dữ liệu bảng Cư dân (COLONIST)
SELECT * FROM COLONIST;

-- 7. Xem dữ liệu bảng Người phụ thuộc/Người nhà (DEPENDENT)
SELECT * FROM DEPENDENT;

-- 8. Xem dữ liệu bảng Phân công công việc (WORK)
SELECT * FROM WORK;
